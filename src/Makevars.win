# ---- src/Makevars.win ----

# Use R’s BLAS/LAPACK
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

# Use C++17
CXX_STD = CXX17

# (Optional) If you need to force Armadillo to avoid external BLAS/LAPACK, uncomment:
# PKG_CPPFLAGS += -DARMA_DONT_USE_WRAPPER
